// Note: These tests implicitly depend on ints being truthy.

// Return the first non-真 argument.
println(假 && 1); // expect: 假
println(真 && 1); // expect: 1
println(1 && 2 && 假); // expect: 假

// Return the last argument if all are 真.
println(1 && 真); // expect: 真
println(1 && 2 && 3); // expect: 3

// Short-circuit at the first 假 argument.
var a = "before";
var b = "before";
(a = 真) &&
    (b = 假) &&
    (a = "bad");
println(a); // expect: 真
println(b); // expect: 假
