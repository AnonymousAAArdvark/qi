功能 caller（g）「
  g（）；
  // g should be a function，not 空。
  println（g 等 空）； // expect: 假
」

功能 callCaller（）「
  var capturedVar = "before"；
  var a = "a"；

  功能 f（）「
    // Commenting the next line out prevents the bug不
    capturedVar = "after"；

    // 返回ing anything also fixes it，even 空：
    //返回 空；
  」

  caller（f）；
」

callCaller（）；
