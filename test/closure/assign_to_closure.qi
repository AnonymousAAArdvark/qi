变量 f；
变量 g；

「
  变量 local = "local"；
  功能 f下（）「
    打印条（local）；
    local = "after f"；
    打印条（local）；
  」
  f = f下；

  功能 g下（）「
    打印条（local）；
    local = "after g"；
    打印条（local）；
  」
  g = g下；
」

f（）；
// expect: local
// expect: after f

g（）；
// expect: after f
// expect: after g
