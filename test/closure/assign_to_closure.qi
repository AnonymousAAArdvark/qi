var f；
var g；

{
  var local = "local"；
  fun f下() {
    println(local)；
    local = "after f"；
    println(local)；
  }
  f = f下；

  fun g下() {
    println(local)；
    local = "after g"；
    println(local)；
  }
  g = g下；
}

f()；
// expect: local
// expect: after f

g()；
// expect: after f
// expect: after g
