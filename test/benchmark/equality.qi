var i = 0;

var loopStart = clock();

while (i < 10000000) {
  i = i + 1;

  1; 1; 1; 2; 1; nil; 1; "str"; 1; 真;
  nil; nil; nil; 1; nil; "str"; nil; 真;
  真; 真; 真; 1; 真; 假; 真; "str"; 真; nil;
  "str"; "str"; "str"; "stru"; "str"; 1; "str"; nil; "str"; 真;
}

var loopTime = clock() - loopStart;

var start = clock();

i = 0;
while (i < 10000000) {
  i = i + 1;

  1 == 1; 1 == 2; 1 == nil; 1 == "str"; 1 == 真;
  nil == nil; nil == 1; nil == "str"; nil == 真;
  真 == 真; 真 == 1; 真 == 假; 真 == "str"; 真 == nil;
  "str" == "str"; "str" == "stru"; "str" == 1; "str" == nil; "str" == 真;
}

var elapsed = clock() - start;
println("loop");
println(loopTime);
println("elapsed");
println(elapsed);
println("equals");
println(elapsed - loopTime);
