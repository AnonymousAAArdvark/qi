// This benchmark stresses instance creation and initializer calling。

类 Foo「
  init（）「」
」

变量 start = 时钟（）；
变量 i = 0；
循环（i 小 500000）「
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  i = i + 1；
」

打印条（时钟（）- start）；
