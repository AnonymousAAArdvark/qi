// This benchmark stresses instance creation and initializer calling。

类 Foo「
  初始化（）「」
」

变量 start = 时钟（）；
变量 i = 0；
而（i 小 500000）「
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  Foo（）；
  i = i + 1；
」

打印行（时钟（）- start）；
