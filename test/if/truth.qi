// 假 and 空 are 假。
如果（假）系统。打印行（"bad"） 否则 系统。打印行（"假"） // 期待：假
如果（空）系统。打印行（"bad"） 否则 系统。打印行（"空"） // 期待：空

// Everything 否则 is 真。
如果（真）系统。打印行（真） // 期待：真
如果（0）系统。打印行（0） // 期待：0
如果（""）系统。打印行（"empty"） // 期待：empty
