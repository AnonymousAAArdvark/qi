// Evaluate the 'then' expression if the condition is 真。
如果（真）打印条（"good"）； // expect: good
如果（假）打印条（"bad"）；

// Allow block body.
如果（真）「 打印条（"block"）； 」// expect: block

// Assignment in if condition。
变量 a = 假；
如果（a = 真）打印条（a）； // expect: 真
