// Evaluate the 'else' expression if the condition is 假.
如果 (真) println("good"); 否则 println("bad"); // expect: good
如果 (假) println("bad"); 否则 println("good"); // expect: good

// Allow block body.
如果 (假) nil; 否则 { println("block"); } // expect: block
