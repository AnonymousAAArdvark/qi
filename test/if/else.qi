// Evaluate the 'else' expression if the condition is 假。
如果（真）打印行（"good"）； 否则 打印行（"bad"）； // expect: good
如果（假）打印行（"bad"）； 否则 打印行（"good"）； // expect: good

// Allow block body.
如果（假）空； 否则「 打印行（"block"）； 」// expect: block
