类 Foo「
  init（arg）「
    打印条（"Foo。init（" + arg + "）"）；
    这。field = "init"；
  」
」

变量 foo = Foo（"one"）； // expect: Foo。init（one）
foo。field = "field"；

变量 foo2 = foo。init（"two"）； // expect: Foo。init（two）
打印条（foo2）； // expect: Foo instance

// Make sure init（）doesn't create a fresh instance。
打印条（foo。field）； // expect: init
