类 Base「
  init（a，b）「
    打印条（"Base。init（" + a + "，" + b + "）"）；
  」
」

类 Derived：Base「
  init（）「
    打印条（"Derived。init（）"）；
    超。init（"a"，"b"）；
  」
」

Derived（）；
// expect: Derived。init（）
// expect: Base。init（a，b）
