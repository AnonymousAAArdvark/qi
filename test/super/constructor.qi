类 Base「
  init（a，b）「
    打印行（"Base。init（" + a + "，" + b + "）"）；
  」
」

类 Derived：Base「
  init（）「
    打印行（"Derived。init（）"）；
    超。init（"a"，"b"）；
  」
」

Derived（）；
// expect: Derived。init（）
// expect: Base。init（a，b）
