类 Base「
  初始化（a，b）「
    打印行（"Base。初始化（" + a + "，" + b + "）"）；
  」
」

类 Derived：Base「
  初始化（）「
    打印行（"Derived。初始化（）"）；
    超。初始化（"a"，"b"）；
  」
」

Derived（）；
// expect: Derived。初始化（）
// expect: Base。初始化（a，b）
