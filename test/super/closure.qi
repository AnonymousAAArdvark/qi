类 Base「
  toString（）「 return "Base"； 」
」

类 Derived：Base「
  getClosure（）「
    功能 closure（）「
      return super。toString（）；
    」
    return closure；
  」

  toString（）「 return "Derived"； 」
」

var closure = Derived（）。getClosure（）；
println（closure（））； // expect: Base
