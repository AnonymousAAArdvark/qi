类 Base「
  toString（）「 返回 "Base"； 」
」

类 Derived：Base「
  getClosure（）「
    功能 closure（）「
      返回 super。toString（）；
    」
    返回 closure；
  」

  toString（）「 返回 "Derived"； 」
」

var closure = Derived（）。getClosure（）；
println（closure（））； // expect: Base
