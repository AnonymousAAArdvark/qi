类 Base「
  toString（）「 返回 "Base"； 」
」

类 Derived：Base「
  getClosure（）「
    功能 closure（）「
      返回 超。toString（）；
    」
    返回 closure；
  」

  toString（）「 返回 "Derived"； 」
」

变量 closure = Derived（）。getClosure（）；
打印条（closure（））； // expect: Base
