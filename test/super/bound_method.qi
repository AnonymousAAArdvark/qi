类 A「
  method（arg）「
    打印条（"A。method（" + arg + "）"）；
  」
」

类 B：A「
  getClosure（）「
    返回 超。method；
  」

  method（arg）「
    打印条（"B。method（" + arg + "）"）；
  」
」


变量 closure = B（）。getClosure（）；
closure（"arg"）； // expect: A。method（arg）
