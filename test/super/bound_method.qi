类 A「
  method（arg）「
    println（"A。method（" + arg + "）"）；
  」
」

类 B：A「
  getClosure（）「
    返回 super。method；
  」

  method（arg）「
    println（"B。method（" + arg + "）"）；
  」
」


var closure = B（）。getClosure（）；
closure（"arg"）； // expect: A。method（arg）
