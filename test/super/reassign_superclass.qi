类 Base「
  method（）「
    println（"Base。method（）"）；
  」
」

类 Derived：Base「
  method（）「
    super。method（）；
  」
」

类 OtherBase「
  method（）「
    println（"OtherBase。method（）"）；
  」
」

var derived = Derived（）；
derived。method（）； // expect: Base。method（）
Base = OtherBase；
derived。method（）； // expect: Base。method（）
