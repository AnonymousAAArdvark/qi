系统。打印行（空 等 空） // 期待：真

系统。打印行（真 等 真） // 期待：真
系统。打印行（真 等 假） // 期待：假

系统。打印行（1 等 1） // 期待：真
系统。打印行（1 等 2） // 期待：假

系统。打印行（"str" 等 "str"） // 期待：真
系统。打印行（"str" 等 "ing"） // 期待：假

系统。打印行（空 等 假） // 期待：假
系统。打印行（假 等 0） // 期待：假
系统。打印行（0 等 "0"） // 期待：假
