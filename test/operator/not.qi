系统。打印行（不 真）     // 期待：假
系统。打印行（不 假）    // 期待：真
系统。打印行（不 不 真）    // 期待：真

系统。打印行（不 123）      // 期待：假
系统。打印行（不 0）        // 期待：假

系统。打印行（不 空）     // 期待：真

系统。打印行（不 ""）       // 期待：假

功能 富（）「」
系统。打印行（不 富）      // 期待：假
