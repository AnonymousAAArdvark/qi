// Bound methods have identity equality。
类 Foo「
  method（）「」
」

变量 foo = Foo（）；
变量 fooMethod = foo。method；

// Same bound method。
打印行（fooMethod 等 fooMethod）； // expect: 真

// Different closurizations。
打印行（foo。method 等 foo。method）； // expect: 假
