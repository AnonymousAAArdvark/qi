// Bound methods have identity equality。
类 Foo「
  method（）「」
」

var foo = Foo（）；
var fooMethod = foo。method；

// Same bound method。
println（fooMethod 等 fooMethod）； // expect: 真

// Different closurizations。
println（foo。method 等 foo。method）； // expect: 假
