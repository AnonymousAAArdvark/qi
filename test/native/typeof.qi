类 测试「
」
系统。打印行（系统。型（测试）） // 期待：类
系统。打印行（系统。型（数字。最小）） // 期待：静态方法
系统。打印行（系统。型（测试（））） // 期待：实例
系统。打印行（系统。型（""）） // 期待：字符串
系统。打印行（系统。型（【】）） // 期待：列表
系统。打印行（系统。型（假）） // 期待：布尔
系统。打印行（系统。型（0）） // 期待：数字
系统。打印行（系统。型（空）） // 期待：空