// * has higher precedence than +。
系统。打印行（2 + 3 * 4） // 期待：14

// * has higher precedence than -。
系统。打印行（20 - 3 * 4） // 期待：8

// / has higher precedence than +。
系统。打印行（2 + 6 / 3） // 期待：4

// / has higher precedence than -。
系统。打印行（2 - 6 / 3） // 期待：0

// 小 has higher precedence than 等。
系统。打印行（假 等 2 小 1） // 期待：真

// 大 has higher precedence than 等。
系统。打印行（假 等 1 大 2） // 期待：真

// 小等 has higher precedence than 等。
系统。打印行（假 等 2 小等 1） // 期待：真

// 大等 has higher precedence than 等。
系统。打印行（假 等 1 大等 2） // 期待：真

// 1 - 1 is not space-sensitive。
系统。打印行（1 - 1） // 期待：0
系统。打印行（1 -1）  // 期待：0
系统。打印行（1- 1）  // 期待：0
系统。打印行（1-1）   // 期待：0

// Using （）for grouping。
系统。打印行（（2 *（6 -（2 + 2）））） // 期待：4
