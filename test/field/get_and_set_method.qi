// Bound methods have identity equality。
类 Foo「
  method（a）「
    打印行（"method"）；
    打印行（a）；
  」
  other（a）「
    打印行（"other"）；
    打印行（a）；
  」
」

变量 foo = Foo（）；
变量 method = foo。method；

// Setting a property shadows the instance method。
foo。method = foo。other；
foo。method（1）；
// 期待：other
// 期待：1

// The old method handle still points to the original method。
method（2）；
// 期待：method
// 期待：2
