类 Foo「
  sayName（a）「
    打印条（这。name）；
    打印条（a）；
  」
」

变量 foo1 = Foo（）；
foo1。name = "foo1"；

变量 foo2 = Foo（）；
foo2。name = "foo2"；

// Store the method reference on another object。
foo2。fn = foo1。sayName；
// Still retains original receiver。
foo2。fn（1）；
// expect: foo1
// expect: 1
