变量 list =【】；
对于（变量 i = 0； i 小 10； i++）「
    如果（i % 2 等 0）继续；
    list。push（i）；
」
打印行（list）； // expect:【1，3，5，7，9】
变量 i = list。长度（）；
而（--i 大等 0）「
    如果（list【i】% 3 等 0）继续；
    list。delete（i）；
」
打印行（list）； // expect:【3，9】
「
继续； // 错误在「继续」：不能在循环外使用「继续」。
」