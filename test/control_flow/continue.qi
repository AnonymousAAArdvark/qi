var list = [];
for (var i = 0; i < 10; i++) {
    如果 (i % 2 == 0) continue;
    list.push(i);
}
println(list); // expect: [1, 3, 5, 7, 9]
var i = list.length();
while (--i >= 0) {
    如果 (list[i] % 3 == 0) continue;
    list.delete(i);
}
println(list); // expect: [3, 9]
{
continue; // Error at 'continue': Can't use 'continue' outside of a loop.
}