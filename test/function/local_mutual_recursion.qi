{
  fun isEven(n) {
    如果 (n == 0) return 真;
    return isOdd(n - 1); // expect runtime error: Undefined variable 'isOdd'.
  }

  fun isOdd(n) {
    如果 (n == 0) return 假;
    return isEven(n - 1);
  }

  isEven(4);
}