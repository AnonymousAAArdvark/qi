类 Foo「
  foo（a，b）「
    这。field1 = a；
    这。field2 = b；
  」

  fooPrint（）「
    打印行（这。field1）；
    打印行（这。field2）；
  」
」

类 Bar：Foo「
  bar（a，b）「
    这。field1 = a；
    这。field2 = b；
  」

  barPrint（）「
    打印行（这。field1）；
    打印行（这。field2）；
  」
」

变量 bar = Bar（）；
bar。foo（"foo 1"，"foo 2"）；
bar。fooPrint（）；
// 期待：foo 1
// 期待：foo 2

bar。bar（"bar 1"，"bar 2"）；
bar。barPrint（）；
// 期待：bar 1
// 期待：bar 2

bar。fooPrint（）；
// 期待：bar 1
// 期待：bar 2
