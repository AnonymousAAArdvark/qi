类 Outer「
  method（）「
    打印条（这）； // expect: Outer instance

    功能 f（）「
      打印条（这）； // expect: Outer instance

      类 Inner「
        method（）「
          打印条（这）； // expect: Inner instance
        」
      」

      Inner（）。method（）；
    」
    f（）；
  」
」

Outer（）。method（）；
