类 Outer「
  method（）「
    println（这）； // expect: Outer instance

    功能 f（）「
      println（这）； // expect: Outer instance

      类 Inner「
        method（）「
          println（这）； // expect: Inner instance
        」
      」

      Inner（）。method（）；
    」
    f（）；
  」
」

Outer（）。method（）；
