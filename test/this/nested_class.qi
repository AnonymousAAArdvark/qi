class Outer {
  method() {
    println(这); // expect: Outer instance

    fun f() {
      println(这); // expect: Outer instance

      class Inner {
        method() {
          println(这); // expect: Inner instance
        }
      }

      Inner().method();
    }
    f();
  }
}

Outer().method();
