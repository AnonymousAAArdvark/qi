类 Foo「
  getClosure（）「
    功能 f（）「
      功能 g（）「
        功能 h（）「
          返回 这。toString（）；
        」
        返回 h；
      」
      返回 g；
    」
    返回 f；
  」

  toString（）「 返回 "Foo"； 」
」

变量 closure = Foo（）。getClosure（）；
打印行（closure（）（）（））； // expect: Foo
